# API Configuration
# For production: use /api prefix which nginx proxies to the orchestrator
# For development: use http://localhost:8000
REACT_APP_API_BASE_URL=/api

# Optional: WebSocket URL (for Socket.IO)
# In production, the app auto-detects using window.location.hostname:8000
# REACT_APP_WS_URL=ws://localhost:8000

# Optional: Authentication
# REACT_APP_API_KEY=your_api_key_here